import React from 'react';

const About = ({ something }) => {
  return(
    <div className="app">
      <main className="front">
        <h1>ABout { something }</h1>
      </main>
    </div>
  );
};

export default About;
